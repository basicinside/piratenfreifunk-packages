#!/bin/sh
logread -f | while read line ; do
  if echo $line | grep -q DHCPACK ; then
    machash=$(echo $line | cut -d ' ' -f 9 | md5sum | cut -d ' ' -f 1)
    echo "$machash" >> /tmp/machashes
  fi
done &
pid=$!
echo $pid > /var/run/machash.pid
